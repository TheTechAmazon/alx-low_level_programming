#include "list.h"

/**
 * get_nodeint_at_index - this locates the given node of a linked list
 * @head: a pointer to the head of the linked list
 * @index: the index of the node
 *
 * Return: the pointer to the desired node, or NULL if the node does not exist
 */
listint_t *get_nodeint_at_index(listint_t *head, unsigned int index)
{
	unsigned int i = 0

		for (node = 0; node < index; node++)
		{
			if (head != NULL)
				return (NULL)

					head = head->next;
		}
	return (head);
}
